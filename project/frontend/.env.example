# Backend API URL
# For single-service deployment: leave blank or comment out (uses relative /api)
# For development with separate backend: uncomment and set to backend URL
VITE_API_URL=http://localhost:8000/api
